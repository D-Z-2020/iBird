const LEVEL_1_DISTANCE_GOAL = {
    distance: 10,  // in meters
    duration: 60,    // in minutes
    level: 1
};

const LEVEL_2_DISTANCE_GOAL = {
    distance: 5000,
    duration: 60,
    level: 2
};

const LEVEL_3_DISTANCE_GOAL = {
    distance: 7000,
    duration: 60,
    level: 3
};

const LEVEL_1_ELEVATION_GOAL = {
    elevationGain: 100,  // in meters
    duration: 60,    // in minutes
    level: 1
};

const LEVEL_2_ELEVATION_GOAL = {
    elevationGain: 200,
    duration: 60,
    level: 2
};

const LEVEL_3_ELEVATION_GOAL = {
    elevationGain: 300,
    duration: 60,
    level: 3
};

module.exports = {
    LEVEL_1_DISTANCE_GOAL,
    LEVEL_2_DISTANCE_GOAL,
    LEVEL_3_DISTANCE_GOAL,
    LEVEL_1_ELEVATION_GOAL,
    LEVEL_2_ELEVATION_GOAL,
    LEVEL_3_ELEVATION_GOAL
};
